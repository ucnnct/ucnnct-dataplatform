# Redis config — déduplication des événements
maxmemory 512mb
maxmemory-policy allkeys-lru

# TTL par défaut pour les clés de dédup (7 jours)
# Appliqué dans le code consumer via EXPIRE

appendonly yes
appendfilename "appendonly.aof"
